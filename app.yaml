runtime: python39
service: website-static # Ganti dengan nama layanan yang Anda inginkan
env_variables:
  BUCKET_NAME: my-bucket-2 
handlers:
  - url: /
    static_files: www/index.html
    upload: www/index.html

  - url: /images
    static_dir: www/images
